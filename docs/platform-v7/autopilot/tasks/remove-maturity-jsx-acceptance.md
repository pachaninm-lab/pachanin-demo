Acceptance:
- `PlatformV7StrategicHomeStory.tsx` contains no `href='#maturity'`.
- It contains no `<section id='maturity'`.
- It does not render `story.trust.items`, `story.trust.metrics`, `story.trust.integrations`, or `story.trust.ladder`.
- The homepage proceeds directly from TAI to FAQ.
- Unit regression passes.
