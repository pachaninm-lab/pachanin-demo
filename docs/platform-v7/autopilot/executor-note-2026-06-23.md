# executor note

After this checkpoint is merged, the next implementation PR should cover the seller cabinet only.

First identify exact seller cabinet files. Then edit only those files.

No broad rewrite. No hidden runtime work.