{
  "schemaVersion": "platform-v7.concurrent-scope.v1",
  "status": "proposed",
  "issue": 5067,
  "branch": "feat/role-eligibility-fns-registry-storage-foundation-5067",
  "allowedPaths": [
    "apps/api/prisma/migrations/20260906013000_role_eligibility_fns_registry_storage_foundation/migration.sql",
    "scripts/role-eligibility-postgres-smoke.sh"
  ],
  "forbiddenPaths": [
    "apps/web/**",
    "infra/**",
    "apps/api/src/modules/registration/**",
    "apps/api/src/modules/role-eligibility/role-eligibility-policy.ts",
    "apps/api/src/modules/role-eligibility/role-eligibility-enforcement-policy.ts",
    ".github/workflows/**"
  ],
  "notes": "STAGED PROPOSAL ONLY. ZERO IMPLEMENTATION AUTHORITY until this exact finite scope is independently reviewed, merged, and separately promoted into immutable trusted base-state authority. #5067 Phase A PostgreSQL foundation only. Add forward-only eligibility storage primitives equivalent to: (1) append-only immutable registry subject versions keyed by source + registry_domain + source_record_id + source version/hash identity, carrying canonical subject identifiers, normalized source payload/hash, parser/schema/source-publication provenance and a non-destructive link to the logical source generation/package identity already accepted elsewhere; (2) compact mutable/rebuildable current-subject pointer rows keyed by source + registry_domain + source_record_id, indexed for current INN/OGRN lookup and pointing to one immutable source-version row; unchanged subjects are never copied merely because a daily authority transition occurs; (3) nullable evidence.source_version_id FK for NEW evidence only, legacy evidence remains null/byte-for-byte readable and no backfill may invent historical lineage. Pointer integrity must be enforced in PostgreSQL, not by caller convention: a current row may reference only an immutable version with the exact same source, registry_domain and source_record_id (use a composite FK/constraint or equivalent fail-closed relational invariant), so an EGRUL pointer cannot be redirected to an EGRIP/other-subject version even by a compromised or buggy runtime caller. For current FNS EGRUL rows, one canonical non-null 10-digit subject INN must not point to multiple distinct current OGRNs; enforce the accepted effective-current INN->OGRN uniqueness at the current-read-model boundary with a domain-specific constraint/index or equivalent atomic invariant, without incorrectly generalizing that rule to unrelated source/domain semantics. Do NOT create a second generation/coverage/finality/read-model authority token in this slice: #5064 owns domain-scoped active authority, coverage, continuity, finality and atomic negative-resolution snapshot semantics. Do NOT switch any application read/write path, activate data, import a real corpus, change source health, or modify current registry_records behavior in Phase A. Legacy registry_records/generations/evidence remain readable and unchanged. Runtime least privilege must be explicit: immutable subject versions SELECT+INSERT only, no UPDATE/DELETE; current pointers SELECT+INSERT+UPDATE only, no DELETE; evidence remains insert-only for the runtime path; no broad table/schema ownership escalation. PostgreSQL smoke must prove empty/existing-small-history migration, legacy evidence with null source_version_id, new evidence can bind an exact immutable version, FK RESTRICT protects referenced versions, cross-domain/cross-subject current-pointer references are rejected by the database, EGRUL current INN->multiple-OGRN conflict is rejected while legitimate unrelated-domain rows are unaffected, current INN/OGRN lookup indexes exist, pointer advancement changes only targeted rows, unchanged pointer rows remain physically untouched, runtime principal cannot mutate/delete immutable versions, current pointer operations are bounded, and schema/migration chain remains drift-safe under existing acceptance. Do not enforce or invent SOURCE_FINALITY, bulk NOT_FOUND, annual/daily continuity, canonical identity policy, status policy, registration behavior, enforcement activation, UI, credentials or production mutation here. Phase B after #5064 will govern application repository/atomic publish/rebuild integration against these accepted tables. REG.RU remains the only production contour. ROLE_ELIGIBILITY_ENFORCEMENT=false. New mandatory recurring code/SaaS cost: 0 RUB. Proposal based on exact main 7f8326322dd15a46df8b63a3f4f14d72a177558d."
}
