{
  "schemaVersion": "platform-v7.concurrent-scope.v1",
  "issue": 4638,
  "branch": "security/remove-dead-mfa-guard-bypass-4638",
  "status": "active",
  "purpose": "Remove the unused RequiresMfaGuard (apps/api/src/common/guards/mfa.guard.ts), which contains an unconditional MFA bypass via an X-MFA-Verified request header with no environment gate, and is imported and used by zero routes anywhere in the API (verified: 0 @UseGuards(RequiresMfaGuard usages, 0 imports of the file, no spec file). Corrects admin.controller.ts's self-report status line, which falsely names this dead class as the live MFA enforcement mechanism; the real, correctly fail-closed mechanism is SettlementFinancialMfaGuard plus policy-engine.service.ts/staff-access.service.ts/signing-authority.policy.ts, none of which this scope touches or weakens. Pure risk reduction; no behavior change for any live route.",
  "authorityBaseExactMain": "e72f2c292151488a8e428d0239086064d8930a61",
  "productionHosting": "REG_RU_VPS_ONLY",
  "newRecurringCostRub": 0,
  "allowedPaths": [
    "docs/platform-v7/autopilot/scopes/remove-dead-mfa-guard-bypass-4638.json",
    "apps/api/src/common/guards/mfa.guard.ts",
    "apps/api/src/modules/admin/admin.controller.ts",
    "docs/security/cryptographic-inventory.json",
    "docs/security/CRYPTOGRAPHIC_INVENTORY.md"
  ],
  "requiredInvariants": [
    "apps/api/src/common/guards/mfa.guard.ts is deleted outright, not weakened or left partially in place.",
    "No other file imports RequiresMfaGuard or SKIP_MFA_KEY from the deleted file, verified by grep across apps/api/src before and after.",
    "admin.controller.ts's self-report line is corrected to name the real, live SettlementFinancialMfaGuard mechanism, not left pointing at the deleted class.",
    "No other security control (SettlementFinancialMfaGuard, policy-engine.service.ts, staff-access.service.ts, signing-authority.policy.ts) is touched or weakened.",
    "TypeScript build (pnpm --filter @pc/api typecheck) passes with zero errors.",
    "No production, REG.RU, DNS or database mutation.",
    "This scope changes no business logic - it is a pure dead-code/security-hygiene removal.",
    "docs/security/cryptographic-inventory.json/CRYPTOGRAPHIC_INVENTORY.md are regenerated via scripts/security/discover-cryptography.mjs only (no hand edits) - deleting mfa.guard.ts shifts the tracked-file count these files record, which the exact-match test in discover-cryptography.test.mjs checks."
  ],
  "acceptance": [
    "apps/api/src/common/guards/mfa.guard.ts no longer exists.",
    "grep for RequiresMfaGuard/SKIP_MFA_KEY/common/guards/mfa.guard across apps/api/src returns zero matches outside this scope's own manifest and issue history.",
    "pnpm --filter @pc/api typecheck passes.",
    "Existing admin/guard/auth/settlement-MFA test suites pass with no regressions (admin.controller.spec.ts, common/guards/*.spec.ts, settlement-financial-mfa.guard.spec.ts, the full src/modules/auth suite)."
  ]
}
