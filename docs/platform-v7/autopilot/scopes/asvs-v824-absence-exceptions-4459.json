{
  "schemaVersion": "platform-v7.concurrent-scope.v1",
  "issue": 4459,
  "branch": "security/asvs-v824-absence-exceptions-4459",
  "status": "active",
  "purpose": "Make the ASVS assessment register truthful and more complete, without moving any requirement to PASS. Two parts, one file. First: an ABSENT_IN_TREE condition read a disable flag as the control it detects - 'geoip' inside a PostHog flag that disables GeoIP rejected the V8.2.4 decision and reported the requirement NOT_ASSESSED instead of the FAIL already measured; named self-verifying exceptions fix that without being able to suppress a real match. Second: V15.2.4 had no decision at all and is now assessed from repository facts and recorded FAIL. Subsequent assessments in the same file cover requirements that had no decision at all: V15.2.4 recorded FAIL, V3.5.4 recorded NOT_ASSESSED with the missing fact named, and V15.3.7 recorded PASS on measured evidence.",
  "authorityBaseExactMain": "a924286d5f012c827b003e3fd54007822121764f",
  "productionHosting": "REG_RU_VPS_ONLY",
  "newRecurringCostRub": 0,
  "allowedPaths": [
    "scripts/security/build-asvs-matrix.mjs",
    "scripts/security/build-asvs-matrix.test.mjs",
    "docs/security/asvs-applicability-decisions.json",
    "docs/platform-v7/autopilot/scopes/asvs-v824-absence-exceptions-4459.json"
  ],
  "requiredInvariants": [
    "The exception mechanism must never move a requirement to PASS: its only permitted effect is to stop a decision being wrongly rejected, restoring the status that decision already recorded. Assessments of previously unassessed requirements may record PASS, but only where repository evidence is measured and expressed as conditions the matrix re-verifies on every run. No status may rest on prose alone.",
    "An exception must never be able to clear a file that also contains an unquoted match; a real adaptive control cannot hide beside a benign string.",
    "An exception must quote the exact line, which is re-read from the real file on every run. A quoted line that changed or no longer matches fails the condition rather than being ignored.",
    "Conditions that declare no exceptions must behave exactly as before.",
    "No product code, dependency, workflow or production configuration may change.",
    "Merge and CI success are not REG.RU production deployment evidence."
  ],
  "ownerRatificationRequired": [
    "Scope authorization. This manifest was written by the implementation agent because no approved concurrent scope existed for this branch and scripts/p7-autopilot-guard.sh blocked. It is the repository's declared source-controlled mechanism, but it is self-declared and requires owner ratification in review before it counts as authorization.",
    "Exception mechanism. Named exceptions are a new capability on ABSENT_IN_TREE conditions. They are constrained and mutation-tested, but they do let a recorded judgement stand against a literal scan, so the owner should confirm the mechanism as well as the two entries it is used for."
  ],
  "acceptance": [
    "V8.2.4 reports FAIL rather than NOT_ASSESSED, and the FAIL count rises rather than falls.",
    "Removing the exceptions restores the previous failure, so they are not vacuous.",
    "Mutating either exception protection fails exactly the test that names it.",
    "V15.2.4 carries conditions that the matrix re-evaluates, not a bare assertion.",
    "No decision anywhere in the register is left rejected: evaluating all conditions yields zero revoked decisions.",
    "The full security suite and the matrix suite stay green."
  ]
}
