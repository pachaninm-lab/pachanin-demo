{
  "schemaVersion": "platform-v7.concurrent-scope.v1",
  "branch": "fix/p0-regru-official-mail-host-3785",
  "status": "active",
  "operationalStatus": "P0_REG_RU_OFFICIAL_MAIL_HOST",
  "issue": 3785,
  "releaseIssue": 3072,
  "title": "Use the official REG.RU mail host for protected mailbox SMTP proof",
  "allowedPaths": [
    ".github/workflows/production-p0-regru-mailbox-smtp-bridge.yml",
    "scripts/production-p0-regru-mailbox-smtp-bridge-v2.sh",
    "scripts/production-p0-regru-mailbox-smtp-proof.py",
    "docs/platform-v7/autopilot/scopes/production-p0-regru-mailbox-smtp-bridge-3785.json"
  ],
  "productionHosting": "REG_RU_EXISTING_INFRASTRUCTURE_ONLY",
  "boundaries": {
    "productionMutation": "ONE_CONTROL_EMAIL_THEN_ROOT_ONLY_AUTH_MAIL_RUNTIME_FILES_AFTER_DELIVERY_PROOF",
    "probeEmailMutation": true,
    "runtimeFileMutationAfterProbeOnly": true,
    "databaseMutation": false,
    "deploymentMutation": false,
    "credentialOutput": false,
    "piiOutput": false,
    "rawEnvironmentOutput": false,
    "rawLogOutput": false,
    "ownerOnly": true,
    "exactMainGuard": true,
    "newRecurringCostRub": 0
  },
  "acceptance": [
    "do not require the protected mailbox login to equal the public sender address",
    "canonicalize the mailbox login domain to ASCII IDNA only for REG.RU SMTP authentication and preserve the protected mailbox login for IMAP",
    "authenticate to the official REG.RU mail.hosting.reg.ru endpoint on TLS port 465 using the already-existing protected mailbox credential",
    "send exactly one controlled message with envelope/header From access@процент-агро.рф to the same protected control mailbox",
    "prove the uniquely marked message is readable back through the existing protected IMAP mailbox before any runtime file mutation",
    "publish no mailbox identity, credential, raw SMTP response, raw IMAP content or protected filesystem path",
    "pin the existing REG.RU DNS and SSH host identity and require root before runtime file mutation",
    "reuse scripts/provision-production-p0-password-reset-runtime.sh with distinct PC_SMTP_USER and PC_MAIL_FROM",
    "perform no database mutation and no deployment mutation",
    "add zero recurring cost"
  ]
}
