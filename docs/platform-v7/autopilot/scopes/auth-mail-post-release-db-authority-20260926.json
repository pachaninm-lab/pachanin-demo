{
  "schemaVersion": "platform-v7.concurrent-scope.v1",
  "branch": "fix/auth-mail-runtime-projection-recovery-20260926",
  "status": "active",
  "authorizedBy": "explicit-user-finish-public-ux-release-20260926",
  "purpose": "Repair the exact auth-mail post-release cutover rc=14 by keeping live API AUTH_DATABASE_URL as the canonical auth-mail datasource and reusing an already-proven pc_auth_mail_runtime credential without password rotation. Read-only production diagnostics on 2026-09-26 proved both the root-only source credential and protected runtime-projection credential authenticate successfully when rebound in memory to the live API auth endpoint, while the historical migration service authority is absent. During bootstrap only, allow a structurally valid stale worker credential to be rebound to the live API endpoint after an in-container login plus exact current_user proof. Keep malformed/mis-owned authority and explicit rotate-db on the existing fail-closed migration-admin path.",
  "authorityBaseExactMain": "b8a3cffbb527468edaf12186b1cf78e71c1880a1",
  "productionHosting": "REG_RU_VPS_ONLY",
  "newRecurringCostRub": 0,
  "allowedPaths": [
    "scripts/provision-production-auth-mail-runtime.sh",
    "scripts/check-production-auth-mail-outbox-cutover.mjs",
    "docs/platform-v7/autopilot/scopes/auth-mail-post-release-db-authority-20260926.json"
  ],
  "requiredInvariants": [
    "No API, Web, database schema, RLS, tenant, role, MFA, SMTP credential, outbox delivery, IR-20 or public UX behavior may change.",
    "The running API container remains the live datasource authority source; neither DATABASE_URL nor AUTH_DATABASE_URL values are published.",
    "The auth-mail runtime database authority must match the running API AUTH_DATABASE_URL endpoint; API DATABASE_URL is not an auth-mail authority substitute.",
    "An existing root-only 0600 source authority with valid pc_auth_mail_runtime shape may be reused directly only when its host, port, database path and query semantically match live API AUTH_DATABASE_URL.",
    "During bootstrap only, an existing structurally valid endpoint-stale source credential may be rebound to the live API AUTH_DATABASE_URL endpoint only after the candidate password authenticates through the exact running API container and SELECT current_user returns exactly pc_auth_mail_runtime.",
    "When source authority is missing, the protected runtime projection may supply the candidate credential only when the runtime parent is root-owned 0700, the projected database-url is root-owned 0444, its principal is pc_auth_mail_runtime with a non-empty password, and the rebound credential passes the same live API login plus current_user proof before atomic installation.",
    "Credential rebinding preserves only the existing pc_auth_mail_runtime username/password and takes scheme, host, port, database path and query exclusively from the protected live API AUTH_DATABASE_URL; no credential value is accepted from a client or command argument.",
    "Malformed or mis-owned source authority must not use projection recovery; explicit rotate-db must never use credential rebinding and remains bounded to migration-admin reconciliation.",
    "Any DB credential rotation still requires exactly one migration service with a valid protected DATABASE_URL and exact migration/API datasource parity before ALTER ROLE executes.",
    "Rebound database URLs are staged root-only, verified before atomic mv, never printed, never passed as process argv, and never included in GitHub evidence.",
    "No client-supplied database, role, tenant or credential authority is introduced.",
    "Production mutation remains confined to the already governed auth-mail authority repair path and is not performed by PR CI."
  ],
  "acceptance": [
    "bash syntax and auth-mail cutover contract checker pass on the exact PR head.",
    "Exact-head contract coverage proves source/projection credential rebinding is bootstrap-only, filesystem-authority checked, principal-shape checked, live-login checked, current_user checked, atomic, secret-safe, and explicit rotate-db remains excluded.",
    "Fresh exact-head CI/security/governance gates are terminal green with zero unresolved review threads.",
    "After exact-SHA merge and REG.RU release, full-stack API/Web/outbox evidence and IR-20 observation pass.",
    "Auth-mail cutover passes on the same exact deployed SHA without FAIL_MIGRATION_DATABASE_AUTHORITY.",
    "Production Mobile Acceptance passes Chromium + WebKit before PUBLIC_EXPERIENCE_V1_FROZEN / FULL_SCOPE_ACCEPTED is recorded."
  ]
}
