{
  "schemaVersion": "platform-v7.concurrent-scope.v1",
  "id": "PRODUCTION-WEB-SINGLE-DISPATCH-AUTHORITY-2026-07-25",
  "branch": "fix/production-web-single-dispatch-authority-v2",
  "status": "completed",
  "maturityBoundary": "production-release-orchestration-idempotency-only",
  "globalProgramStateUnchanged": "This scope did not advance application, banking, integration or TAI production maturity. It authorized coordination between the primary exact-SHA push release and a fail-closed image-gate fallback.",
  "allowedPaths": [
    ".github/workflows/production-web-exact-sha.yml",
    ".github/workflows/production-web-exact-sha-image-gate.yml"
  ],
  "forbiddenPaths": [
    "apps/api/**",
    "apps/web/**",
    "packages/**",
    "infra/**",
    "pnpm-lock.yaml",
    "production credentials"
  ],
  "requiredEvidence": [
    "Dockerfile.web changes retain one primary automatic exact-SHA release authority",
    "The image gate waits for the exact canonical OCI revision and the matching primary push run",
    "Fallback dispatch is allowed only when the primary Wait for exact canonical image step failed before SSH or production mutation",
    "Primary success, in-flight, post-mutation failure and inconclusive states never cause a duplicate fallback deploy",
    "No application runtime, deployment script, Dockerfile or credential was changed"
  ]
}
