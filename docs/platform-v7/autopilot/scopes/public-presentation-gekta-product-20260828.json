{
  "schemaVersion": "platform-v7.concurrent-scope.v1",
  "branch": "fix/public-presentation-gekta-product-20260828",
  "status": "active",
  "purpose": "Replace the existing public presentation PDF in place with the verified revised 14-page «Прозрачная Цена + ГЕКТА» deck, preserving the established download URL, visible CTA, middleware boundary and all authentication/business behavior.",
  "allowedPaths": [
    "apps/web/public/downloads/prozrachnaya-tsena-presentation.pdf",
    "apps/web/tests/unit/platformV7PresentationDownload.test.ts"
  ],
  "requiredBehavior": [
    "The existing public download URL /downloads/prozrachnaya-tsena-presentation.pdf remains unchanged and repeatedly returns the same valid, unencrypted, web-readable PDF with exactly 14 pages.",
    "The exact revised payload is 352861 bytes with SHA-256 6325b4415fec45485094d2572947651df2f0ce375ea9b771ad34743307bd0458.",
    "The revised deck preserves the established 16:9 visual system and presents ГЕКТА explicitly as a standalone agricultural AI product with its own /gekta entry point.",
    "The revised deck also presents ГЕКТА as an analysis capability available inside the «Прозрачная Цена» deal workflow without merging the two products into one undefined offering.",
    "The Ministry of Agriculture wording inherited from the supplied source remains unchanged and is not expanded into an unverified endorsement, integration or production claim.",
    "The existing presentation CTA, middleware exact public boundary, attachment filename, authentication, RBAC, tenant and organization behavior remain unchanged.",
    "The focused unit test pins the exact PDF byte length and SHA-256 digest, verifies PDF framing and the 14-page payload marker, and reads the payload twice to prove deterministic repeatable download content."
  ],
  "forbiddenChanges": [
    "web UI, layout, CTA placement, middleware, authentication, RBAC, tenant, role or organization behavior",
    "API, database schema or migrations, packages, lockfiles, production workflows, hosting topology, secrets, volumes, networks or external services",
    "any generic download proxy, user-controlled path, broad /downloads bypass or dynamic external fetch",
    "claims that a bank, government system, EDI provider or other external integration is connected without verified runtime evidence",
    "claims that the platform guarantees payment, releases money itself, is production-ready or fully integrated",
    "changes outside the exact two implementation paths listed above"
  ]
}
