# Staff Control Center acceptance marker

The exact implementation branch is synchronized into the one-shot browser evidence branch before acceptance. This marker contains no runtime behavior and may be removed with the temporary concurrent scope during cleanup.
