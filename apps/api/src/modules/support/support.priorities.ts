/**
 * Единственный источник правды о приоритетах тикета поддержки.
 *
 * Отдельный файл, потому что список нужен одновременно типу в сервисе и
 * декоратору в DTO. Урок из #4991: вторая копия того же перечня расходится
 * молча — там я объявил список scope заново прямо под комментарием о том, что
 * ничего не дублируется. Здесь массив один, а тип выводится из него.
 *
 * Порядок значим: индекс в этом массиве И ЕСТЬ ранг тикета в очереди
 * поддержки — см. `priorityRank` в support.service.ts.
 *
 * В первой версии этой правки та же фраза стояла здесь, будучи НЕПРАВДОЙ:
 * `listQueue` держал собственный объект `{ CRITICAL: 0, HIGH: 1, ... }`, и
 * комментарий описывал устройство, которого не было. Найдено ревью.
 */
export const TICKET_PRIORITIES = ['CRITICAL', 'HIGH', 'MEDIUM', 'LOW'] as const;

export type TicketPriority = (typeof TICKET_PRIORITIES)[number];
