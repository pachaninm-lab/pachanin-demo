import { AppShell } from '../../components/app-shell';

export default function RuntimeOpsPage() {
  return (
    <AppShell title="Runtime ops" subtitle="Outbox, commands, locks, janitor, watchdog and stream operations">
      <div className="card">Runtime operations cockpit placeholder.</div>
    </AppShell>
  );
}
