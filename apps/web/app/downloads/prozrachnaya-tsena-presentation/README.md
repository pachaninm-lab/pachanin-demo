# Public presentation download

This route serves the public presentation from the embedded base64 asset in `chunks/00.ts`.
The download is intentionally public and repeatable; it does not grant platform access or disclose protected data.
