import { AppShell } from '../../components/app-shell';

export default function PartnerRailPage() {
  return (
    <AppShell title="Partner rail" subtitle="Партнёры, bridge-model, bank/NTB/region integrations and operating owners">
      <div className="card">Partner rail placeholder.</div>
    </AppShell>
  );
}
