import { AppShell } from '../../components/app-shell';

export default function UatPage() {
  return (
    <AppShell title="UAT" subtitle="Release health, provider health and test contour snapshot">
      <div className="card">UAT dashboard placeholder.</div>
    </AppShell>
  );
}
