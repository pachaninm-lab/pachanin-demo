import { AppShell } from '../../../components/app-shell';

export default function CompanyOnboardingPage() {
  return (
    <AppShell title="Company onboarding" subtitle="Юрлицо, ИНН, signers, legal docs and admission status">
      <div className="card">Company onboarding placeholder.</div>
    </AppShell>
  );
}
