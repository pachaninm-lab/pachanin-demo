'use client';

/* Rolled back: the unified header experiment is intentionally disabled.
   Keep a no-op export temporarily so any stale imports fail safe instead of
   mounting a second header. The shell no longer imports this component. */
export function PlatformV7UnifiedHeader() {
  return null;
}
