'use client';

import type { ReactNode } from 'react';
import '@/app/platform-v7/_styles/full-platform';

export function PlatformV7FullStyleBoundary({ children }: { children: ReactNode }) {
  return children;
}
