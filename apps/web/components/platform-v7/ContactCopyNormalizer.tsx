'use client';

/**
 * Compatibility component retained for existing imports.
 * Visitor-visible contact copy is owned by ContactClient source rendering;
 * client-side DOM rewriting is intentionally disabled.
 */
export function ContactCopyNormalizer() {
  return null;
}
