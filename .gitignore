node_modules/
dist/
.next/
coverage/
playwright-report/
test-results/
.env
.env.local
*.log
*.tsbuildinfo
var/runtime/
var/tmp/
.DS_Store
package-lock.json
*.db
*.db-journal
# migrations tracked in git for Railway prisma migrate deploy

# Python bytecode and local tool caches. Generated on every run; never committed.
__pycache__/
*.py[cod]
.coverage
.ruff_cache/
.mypy_cache/
.pytest_cache/
*.egg-info/

# Written by tests/test_model_benchmark_admission_v2.py, which deliberately
# keeps its scratch authority outside model-artifacts so the canonical
# apps/tai/model-artifacts/model-bundle-authority.v2.json is never rewritten.
# It has already slipped into a commit once; ignoring it keeps that accidental.
/apps/tai/model-bundle-authority.v2.json
