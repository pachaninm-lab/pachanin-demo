name: Gekta P0 SPEED G3A Retest Materializer

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/gekta-p0-speed-g3a-materialize.yml'
      - 'scripts/materialize-gekta-p0-speed-g3a.mjs'
      - 'docs/platform-v7/autopilot/scopes/gekta-p0-speed-g3a-3896.json'

permissions:
  contents: write

concurrency:
  group: gekta-p0-speed-g3a-retest-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  materialize:
    if: >-
      github.head_ref == 'perf/gekta-p0-speed-g3a-retest-3896' &&
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: perf/gekta-p0-speed-g3a-retest-3896
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version

      - name: Reconfirm exact retest authority
        run: |
          set -euo pipefail
          git fetch --no-tags origin main
          test "$(git rev-parse origin/main)" = '2f27deeceb22e5b0005c1d1c3ab98ac7c24b7604'
          node -e "const s=require('./docs/platform-v7/autopilot/scopes/gekta-p0-speed-g3a-3896.json'); if(s.authorityBaseExactMain!=='2f27deeceb22e5b0005c1d1c3ab98ac7c24b7604'||s.productionEvidence.beforeRunId!==31839778862) process.exit(1)"

      - name: Materialize exact bounded source changes
        run: node scripts/materialize-gekta-p0-speed-g3a.mjs

      - name: Prove materialized scope and self-delete one-shot files
        run: |
          set -euo pipefail
          grep -Fq 'Begin with one short self-contained complete sentence that directly answers the question' \
            apps/api/src/modules/ai-insights/restricted-public-qwen.service.ts
          grep -Fq "not.toContain('Begin with one short self-contained complete sentence" \
            apps/api/src/modules/ai-insights/restricted-public-qwen.service.spec.ts
          git rm .github/workflows/gekta-p0-speed-g3a-materialize.yml scripts/materialize-gekta-p0-speed-g3a.mjs
          GITHUB_HEAD_REF=perf/gekta-p0-speed-g3a-retest-3896 BASE_REF=origin/main bash scripts/p7-autopilot-guard.sh
          git diff --check

      - name: Commit exact candidate back to governed branch
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add apps/api/src/modules/ai-insights/restricted-public-qwen.service.ts \
                  apps/api/src/modules/ai-insights/restricted-public-qwen.service.spec.ts \
                  docs/platform-v7/autopilot/scopes/gekta-p0-speed-g3a-3896.json
          if git diff --cached --quiet && git diff --quiet; then
            echo 'Candidate already materialized.'
            exit 0
          fi
          git commit -m 'Gekta P0 SPEED G3A retest: shorten first safe answer block'
          git push origin HEAD:perf/gekta-p0-speed-g3a-retest-3896
