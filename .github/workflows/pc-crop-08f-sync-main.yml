name: PC-CROP-08F exact-main synchronization

on:
  push:
    branches: [main]
    paths: [.github/workflows/pc-crop-08f-sync-main.yml]
  workflow_dispatch:
  issue_comment:
    types: [created]
  pull_request:
    types: [ready_for_review]
    branches: [main]

permissions:
  contents: write
  issues: read
  pull-requests: read

concurrency:
  group: pc-crop-08f-exact-main-sync
  cancel-in-progress: true

env:
  BRANCH: agent/pc-crop-08f-sdiz-projection
  DATABASE_URL: postgresql://postgres:postgres@localhost:5432/grainflow?schema=public
  SYNC_LOG_DIR: artifacts/pc-crop-08f-sync

jobs:
  synchronize:
    if: >-
      github.event_name == 'push' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && github.event.pull_request.number == 3180) ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.number == 3180 &&
       github.event.comment.user.login == github.repository_owner &&
       github.event.comment.body == '/pc-crop-08f sync-main')
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: grainflow
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U postgres -d grainflow"
          --health-interval 5s --health-timeout 5s --health-retries 20

    steps:
      - name: Checkout exact target branch
        uses: actions/checkout@v4
        with:
          ref: ${{ env.BRANCH }}
          fetch-depth: 0

      - name: Merge runtime-exact main
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$SYNC_LOG_DIR"
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git fetch --no-tags origin main "$BRANCH"
          expected_branch_head="$(git rev-parse "origin/$BRANCH")"
          [[ "$(git rev-parse HEAD)" == "$expected_branch_head" ]]
          base_main_sha="$(git rev-parse origin/main)"
          printf 'EXPECTED_BRANCH_HEAD=%s\nBASE_MAIN_SHA=%s\n' \
            "$expected_branch_head" "$base_main_sha" >> "$GITHUB_ENV"
          git merge --no-ff --no-commit "$base_main_sha"
          git status --short | tee "$SYNC_LOG_DIR/post-merge-status.txt"

      - name: Apply final SDIZ remediation
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/scripts/pc-crop-08f-sdiz-remediation.py
          python3 .github/scripts/pc-crop-08f-prisma-void-lock-fix.py
          git status --short | tee "$SYNC_LOG_DIR/post-remediation-status.txt"

      - uses: pnpm/action-setup@v4
        with:
          version: '10.2.1'

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install frozen dependencies
        shell: bash
        run: |
          set -euo pipefail
          pnpm install --frozen-lockfile 2>&1 | tee "$SYNC_LOG_DIR/install.log"

      - name: Normalize and validate Prisma authority
        shell: bash
        run: |
          set -euo pipefail
          node scripts/pc-crop-08f/normalize-signature-authority.mjs 2>&1 | tee "$SYNC_LOG_DIR/normalize-signature.log"
          pnpm --filter @pc/api exec prisma format 2>&1 | tee "$SYNC_LOG_DIR/prisma-format.log"
          pnpm --filter @pc/api exec prisma validate 2>&1 | tee "$SYNC_LOG_DIR/prisma-validate.log"
          git diff --exit-code -- apps/api/prisma/schema.prisma 2>&1 | tee "$SYNC_LOG_DIR/schema-diff.log"

      - name: Deploy migrations and generate Prisma client
        shell: bash
        run: |
          set -euo pipefail
          pnpm --filter @pc/api exec prisma migrate deploy 2>&1 | tee "$SYNC_LOG_DIR/migrate-deploy.log"
          pnpm --filter @pc/api exec prisma generate 2>&1 | tee "$SYNC_LOG_DIR/prisma-generate.log"

      - name: Typecheck synchronized API
        shell: bash
        run: |
          set -euo pipefail
          pnpm --filter @pc/api typecheck 2>&1 | tee "$SYNC_LOG_DIR/typecheck.log"

      - name: Run synchronized SDIZ contract tests
        shell: bash
        run: |
          set -euo pipefail
          pnpm --filter @pc/api exec jest --runInBand --runTestsByPath \
            src/modules/regulatory-integration/fgis-grain/fgis-grain-sdiz-projection.contract.spec.ts \
            2>&1 | tee "$SYNC_LOG_DIR/contract-tests.log"

      - name: Run synchronized PostgreSQL SDIZ acceptance
        shell: bash
        run: |
          set -euo pipefail
          PC_CROP_08F_POSTGRESQL=1 pnpm --filter @pc/api exec jest \
            --config test/industrial/jest.config.json \
            test/industrial/fgis-grain-sdiz-projection.e2e-spec.ts \
            --runInBand 2>&1 | tee "$SYNC_LOG_DIR/postgresql-acceptance.log"

      - name: Upload synchronization diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pc-crop-08f-sync-${{ github.run_id }}
          path: ${{ env.SYNC_LOG_DIR }}
          if-no-files-found: warn
          retention-days: 30

      - name: Publish one clean exact-main commit
        shell: bash
        run: |
          set -euo pipefail
          git fetch --no-tags origin main "$BRANCH"
          [[ "$(git rev-parse origin/main)" == "$BASE_MAIN_SHA" ]] || {
            echo 'main moved during synchronization' >&2
            exit 29
          }
          [[ "$(git rev-parse "origin/$BRANCH")" == "$EXPECTED_BRANCH_HEAD" ]] || {
            echo 'target branch moved during synchronization' >&2
            exit 30
          }
          git add -A
          git commit -m 'chore(pc-crop-08f): materialize synchronized tree'
          git reset --soft "$BASE_MAIN_SHA"
          git add -A
          git diff --cached --name-only | sort > /tmp/changed-files.txt
          cat /tmp/changed-files.txt
          if grep -Ev '^(\.github/workflows/pc-crop-08f\.yml|apps/api/prisma/schema\.prisma|apps/api/prisma/migrations/20260724190000_fgis_grain_sdiz_projection/migration\.sql|apps/api/src/modules/regulatory-integration/fgis-grain/fgis-grain-sdiz-projection\.(contract\.spec|contract|repository)\.ts|apps/api/src/modules/regulatory-integration/regulatory-integration\.module\.ts|apps/api/test/industrial/fgis-grain-sdiz-projection\.e2e-spec\.ts|docs/platform-v7/autopilot/scopes/pc-crop-08f-sdiz-projection\.json|scripts/pc-crop-08f/(normalize-signature-authority|verify)\.mjs)$' /tmp/changed-files.txt; then
            echo 'final diff exceeds PC-CROP-08F scope' >&2
            exit 31
          fi
          [[ "$(wc -l < /tmp/changed-files.txt | tr -d ' ')" == '11' ]]
          git commit -m 'PC-CROP-08F: harden durable SDIZ authority on exact main'
          git push --force-with-lease="refs/heads/${BRANCH}:${EXPECTED_BRANCH_HEAD}" \
            origin "HEAD:refs/heads/${BRANCH}"
