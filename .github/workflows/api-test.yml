name: API Tests

on:
  push:
    branches: [main, 'claude/**']
    paths:
      - 'apps/api/**'
      - 'shared/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
  pull_request:
    paths:
      - 'apps/api/**'
      - 'shared/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
  workflow_dispatch:

concurrency:
  group: api-test-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    name: API unit tests + TypeScript check
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.2.1
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - name: API typecheck
        run: |
          set -o pipefail
          pnpm --filter @pc/api typecheck 2>&1 | tee /tmp/api-typecheck.log
      - name: Upload API typecheck log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: api-typecheck-log
          path: /tmp/api-typecheck.log
      - name: API tests
        run: |
          set -o pipefail
          pnpm --filter @pc/api test 2>&1 | tee /tmp/api-test.log
      - name: Upload API test log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: api-test-log
          path: /tmp/api-test.log
